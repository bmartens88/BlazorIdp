@inject NavigationManager NavigationManager

@code{

    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo($"Account/Login?redirectUrl={Uri.EscapeDataString(NavigationManager.Uri)}", forceLoad: true);
    }

}